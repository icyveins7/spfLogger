# spfLogger

A simple, header-only C++ library that `printf` lovers will enjoy.

The priority is on short, concise code that feels like `printf`.

# How to use it

An example is included in the `example/` directory; a `CMakeLists` has been provided to build it.

Otherwise, simply drop this header in any source file and use it.
